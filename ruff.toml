# ruff 配置：PEP 8 代码风格检查（python -m ruff check .）
# RUF012 针对 Django Meta 类中的列表属性（fields/widgets/ordering 等），
# 属于 Django 惯例写法，故忽略。
line-length = 100
target-version = "py312"
exclude = ["*/migrations/*"]

[lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["RUF012"]
